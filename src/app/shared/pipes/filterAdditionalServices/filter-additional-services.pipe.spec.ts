import { FilterAdditionalServicesPipe } from './filter-additional-services.pipe';

describe('FilterAdditionalServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdditionalServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
