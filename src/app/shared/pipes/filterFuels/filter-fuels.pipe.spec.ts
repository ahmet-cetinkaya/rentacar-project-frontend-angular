import { FilterFuelsPipe } from './filter-fuels.pipe';

describe('FilterFuelsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFuelsPipe();
    expect(pipe).toBeTruthy();
  });
});
