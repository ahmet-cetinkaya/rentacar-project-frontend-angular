import { FilterModelListPipe } from './filter-models-list.pipe';

describe('FilterModelListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModelListPipe();
    expect(pipe).toBeTruthy();
  });
});
