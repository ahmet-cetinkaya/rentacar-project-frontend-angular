import { FilterRentalBranchesPipe } from './filter-rental-branches.pipe';

describe('FilterRentalBranchesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRentalBranchesPipe();
    expect(pipe).toBeTruthy();
  });
});
