import { FilterTransmissionsPipe } from './filter-transmissions.pipe';

describe('FilterTransmissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTransmissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
